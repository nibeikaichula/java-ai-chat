version: '3.8'

services:
  java-ai-chat:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      SPRING_PROFILES_ACTIVE: prod
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped